# ============================================================
# 这个文件决定「上传 GitHub 时哪些东西不带上去」。
# 下面这些都是私密文件，绝对不能公开 —— 别删这个文件！
# ============================================================

# 管理员密码哈希 + 登录令牌：泄露了别人就可能破解你的后台密码
data/admin-pass.json
data/auth-tokens.json

# 文章底稿：里面有你没发布的草稿。
# 公开网站只需要自动编译出来的 site.js / articles.js / posts/，不需要它。
data/db.json
data/db.json.tmp
data/db.json.broken-*

# 本地备份（包含底稿和草稿，也没必要传）
备份/

# 依赖目录（体积大，线上静态站不需要）
node_modules/

# macOS 系统垃圾文件
.DS_Store
