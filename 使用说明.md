# 唐维西的网站 —— 使用说明

给完全不懂代码的人看的。全文不需要你写任何一行代码。

---

## 一、每次要用时怎么开

**双击「启动.command」**。会弹出一个黑色终端窗口，浏览器自动打开你的网站。

- 那个黑色窗口 **别关**，它就是网站的引擎。用完了再关。
- 第一次双击如果 Mac 拦下来说「来自身份不明的开发者」：
  **右键点它 → 选「打开」→ 再点一次「打开」**。只需要做这一次。

打开后有两个地址：

| 地址 | 是什么 |
|---|---|
| http://localhost:4321 | 你的网站首页（读者看到的样子） |
| http://localhost:4321/admin.html | 后台（你写文章的地方） |

首页 **右下角有个「✍️ 写文章」按钮**，点它就进后台，不用记地址。

**第一次进后台会让你设置一个管理员密码**，以后每次进后台都要输（同一台电脑 30 天内免输）。
请记好它 —— 忘了的话见最下面的常见问题。

---

## 二、写一篇文章

1. 后台左上角点 **「新建文章」**（新文章默认是草稿，读者看不到）
2. 填标题 → 按回车，光标自动跳到正文
3. 正文里 **输入一个 `/`**，弹出菜单，选标题 / 列表 / 图片 / 代码 / 表格…
4. 写完点右上角 **「保存」**，想让读者看到就再点 **「发布」**

就这样。日期、阅读时间、字数、摘要、封面全都自动填好了。

> **改动什么时候会出现在网站上？** 只有点「保存」（或按 `⌘S`）的那一刻。
> 平时打字的内容会自动**暂存**在这台电脑的浏览器里，防止丢稿，
> 但不会更新到网站 —— 所以可以放心大胆地改，改砸了只要不点保存就行。

### 排版的三种方式

**方式一：`/` 菜单**（最省心）
输入 `/`，上下键选，回车确认。也可以接着打字过滤，比如 `/引用`。

**方式二：行首快捷符号**（最快）

| 打这个 + 空格 | 变成 |
|---|---|
| `#` | 大标题 |
| `##` | 中标题（会自动进文章目录） |
| `###` | 小标题（会自动进文章目录） |
| `-` 或 `*` | 圆点列表 |
| `1.` | 编号列表 |
| `>` | 引用 |
| `[]` | 待办清单 |
| ` ``` ` | 代码块 |
| `---` | 分割线 |

**方式三：选中文字**
用鼠标划选一段文字，上方浮出小工具条：加粗、斜体、高亮、行内代码、加链接、转成标题。

### 放图片

三选一：**拖进去** / **直接粘贴** / `/` 选「图片」。
图片会自动存进 `uploads` 文件夹，不会因为你挪动原图而失效。

### 快捷键

| 按键 | 作用 |
|---|---|
| `⌘S` | 立刻保存 |
| `⌘K` | 给选中的文字加链接 |
| `⌘1` `⌘2` `⌘3` | 转成一/二/三级标题 |
| `⌘0` | 转回普通正文 |
| `⌘⇧H` | 高亮 |
| `⌘回车` | 从代码块里跳出来 |
| `/`（在首页） | 光标跳到搜索框 |

> 首页的搜索框能搜**正文全文**，不止标题 —— 只记得文章里某个词也能找到它。

---

## 三、后台会自动帮你填的东西

| 项目 | 规则 |
|---|---|
| 日期 | 新建时填今天，可以手动改 |
| 阅读时间 | 中文按 400 字/分钟，图片每张算 12 秒，代码另外加权。你自己改过之后，就不再自动覆盖你的数字；想还给它自动算，点旁边的「自动」按钮 |
| 字数 | 中文按字数、英文按词数，底部实时显示 |
| 摘要 | 你不填就自动截取正文开头 90 字 |
| 封面 | 你不传就按标题生成一张固定配色的抽象封面（同一篇永远同一个颜色） |
| NEW 徽章 | 发布 14 天内自动出现，过期自动消失 |
| 暂存 | 停手半秒自动暂存到本机（防丢稿）；点「保存」或 `⌘S` 才会更新到网站 |

---

## 四、草稿 / 发布 / 隐藏 / 置顶

- **新建的文章默认是草稿**，只有你在后台看得见，首页不显示
- 点「发布」它才出现在首页；点「转为草稿」可以撤回，内容不丢
- 点「**隐藏**」：文章立刻从首页、归档、RSS 里消失，但保留着「已发布」的状态，
  点「取消隐藏」马上恢复 —— 适合暂时不想给人看、又不想动内容的时候
- 「预览」按钮看到的是你**当前编辑中（还没保存）**的样子，随便点，不会影响网站
- 打开「置顶」开关，这篇会永远排在首页最前面

---

## 五、改网站的名字、头像、简介、链接、标签库

后台左下角 **「⚙︎ 网站设置」**，你在网站上看到的一切「个人信息」都在这里改：

- **网站名字**（就是你的用户名，比如「唐维西」）和一句话简介（中英文各一份，英文不填就自动用中文）
- **头像 / Logo**：GitHub 图标 / 文字圆标 / 上传你自己的照片 / 不显示
  - 弹窗左边有个实时预览，点它就能直接传图
  - 首页、关于页、后台左上角用的都是同一个头像，改一处全变
- 页脚文字
- 网站正式网址（**以后上线了再填**，用于 RSS 订阅和搜索引擎收录，本地用不用管）
- **首页那排小图标链接**（GitHub、邮箱、微信、知乎、B站、小红书、X…），随便加、随便删
  - 邮箱要写成 `mailto:你的@邮箱.com`
- **标签库**（教程 / 工具 / 随笔 / 思考…）：写文章加标签时推荐的备选词，随便加、随便删
  - 首页筛选条只显示文章实际用到的标签，所以改标签库不会影响已有文章

## 五点五、「关于我」页面

首页右上角有 **「关于」**，是一个专门介绍你自己的页面 —— 正经的个人网站都有这一页。

想写它：后台左下角点 **「🙂 关于页」**，用法和写文章完全一样（`/` 唤菜单、能放图）。
写完点保存，「关于」页立刻更新。页面上的头像和社交链接会自动用「网站设置」里那套。

## 五点五五、友链页

「友链」是把朋友们的网站列成一页卡片（页脚里能点到），中文博客圈的老传统。
加友链：后台「⚙︎ 网站设置」拉到最底下，填名字、网址、一句话介绍就行，
头像不用传 —— 会按名字自动配一个渐变色圆标。

## 五点六、归档页

首页右上角的 **「归档」**，把你所有文章按年份排成一张时间线，方便翻旧文。
它是全自动的，你什么都不用做。

---

## 六、从别的网页导入文章

后台上方「⇩ 从网页导入」，贴一个网址，会把那个页面的正文抓下来，
自动去掉导航、广告、侧边栏，清洗成干净格式，图片也会存到本地。

> 抓完 **一定要自己通读一遍再发**。转载别人的内容记得注明出处。

---

## 七、文件都放在哪

```
唐维西的网站/
├── 启动.command      ← 双击这个
├── index.html        ← 首页
├── article.html      ← 文章阅读页
├── about.html        ← 「关于我」页
├── archive.html      ← 归档页
├── links.html        ← 友链页
├── 404.html          ← 走错路时看到的页面
├── admin.html        ← 后台
├── server.js         ← 引擎（别删）
├── feed.xml          ← RSS 订阅（自动生成）
├── sitemap.xml       ← 站点地图（自动生成，给搜索引擎看的）
├── robots.txt        ← 自动生成，给搜索引擎看的
├── assets/           ← 样式和代码
├── uploads/          ← 你上传的所有图片
└── data/
    ├── db.json       ← 你所有的文章都在这一个文件里 ⭐
    ├── site.js       ← 自动生成，别手改
    ├── articles.js   ← 自动生成，别手改
    └── posts/        ← 自动生成，别手改
```

**备份最省事的办法**：后台左下角点 **「⤓ 备份」**，会把你的全部文字（db.json）
和全部图片（uploads）打成一个 zip，存进网站文件夹里的 `备份/` 目录，自动保留最近 10 份。
换电脑或者出问题时，把 zip 解开、盖回原位置就行。
（当然，整个 `唐维西的网站` 文件夹拷去移动硬盘，永远是最稳的。）

---

## 八、常见问题

**Q：后台打不开，说"连不上本地服务器"？**
A：你多半是直接双击了 `admin.html`。必须先双击「启动.command」，再从首页右下角进后台。

**Q：不开服务器能看自己的网站吗？**
A：能。直接双击 `index.html` 就能读所有文章（只是不能写）。

**Q：文章不小心删了能找回来吗？**
A：不能。删除有确认弹窗，看清楚再点。重要文章建议偶尔拷一份 `data/db.json` 留底。

**Q：以后想让别人也能访问怎么办？**
A：这套网站是纯静态的，整个文件夹传到 GitHub Pages / Vercel / Netlify 就能上线，
不需要数据库、不需要服务器。**上传前必读下面第九节**。

---

## 九、上传 GitHub 前必读（密码和隐私）

文件夹里有几个**私密文件，绝对不能传到公开仓库**：

| 文件 | 里面是什么 |
|---|---|
| `data/admin-pass.json` | 你后台密码的加密指纹。公开了别人就能离线慢慢试出你的密码 |
| `data/auth-tokens.json` | 登录令牌，等于「免密码进后台的钥匙」 |
| `data/db.json` | 文章底稿，**包括你没发布的草稿** |

好消息：项目里已经放了一个 `.gitignore` 文件（隐藏文件，看不见很正常），
用 git 上传时这三样会**自动被跳过**，你不用做任何事 —— 但**别删掉 .gitignore**。
如果你不用 git、而是手动拖文件上传，记得跳过整个 `data/db.json` 和上面两个文件
（`data/site.js`、`data/articles.js`、`data/posts/` 是要传的，公开网站靠它们显示文章）。

关于密码还有三件事要知道：

1. **传上 GitHub 的网站没有后台。** 后台的保存、上传功能都靠你电脑上的 `server.js`，
   GitHub Pages 上只有静态网页 —— 访客连登录的入口都没有，不存在「网上有人破解你后台」这回事。
   密码只在你自己电脑上、开着服务器时才起作用。
2. 密码的加密方式已经升级成 **scrypt**（一种故意算得又慢又耗内存的算法，
   就算指纹文件泄露，逐个试密码的成本也极高）。你下次登录后台时会自动完成升级，无感。
3. 但再好的加密也救不了太短的密码 —— **4 位纯数字总共只有一万种组合**，泄露了几秒就能试完。
   建议改成 8 位以上、带字母的密码：删掉 `data/admin-pass.json`，重启服务器，后台会让你重新设一个。

**Q：端口 4321 被占用了？**
A：程序会自动往后找 4322、4323…，看终端窗口里打印的地址就行。
